"use client";
import {useState} from "react";
import {createClient} from "@supabase/supabase-js";
const db=createClient("https://pixlqytdhgxcfgagwijy.supabase.co","sb_publishable_twXglFUicR3kgQ3fTO3pJA_Ji3qwIwm");
export default function Business(){const[sent,setSent]=useState(false),[status,setStatus]=useState("");async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget),row:any=Object.fromEntries(f);setStatus("Submitting…");const{data:{user}}=await db.auth.getUser();if(!user){setStatus("Sign in from Account first so approved access can be attached to you.");return}const{error}=await db.from("organization_claims").insert({organization_name:row.organization_name,website:row.website||null,contact_email:row.contact_email,message:row.message||null,user_id:user.id});if(error)setStatus(error.message);else{setSent(true);setStatus("Received. We’ll review the organization before anything is verified.")}}return <main className="accountPage"><a href="/">← Free Near Me</a><section className="accountCard"><h1>Put your free stuff on the map.</h1><p>Libraries, cities, museums, nonprofits, gyms, restaurants, retailers and community groups can submit legitimately free programs and events. Sponsored placement never changes whether something is labeled FREE.</p>{sent?<b>{status}</b>:<form className="businessForm" onSubmit={submit}><label>Organization<input name="organization_name" required maxLength={160}/></label><label>Website<input name="website" type="url"/></label><label>Contact email<input name="contact_email" type="email" required/></label><label>What do you offer?<textarea name="message" rows={5} maxLength={1000} placeholder="Free events, workshops, admission days, community services…"/></label><button>Request organization access</button></form>}<a className="detailPrimary" href="/organization">Organization dashboard</a><a className="detailPrimary" href="/submit">Submit one free find instead</a></section></main>}