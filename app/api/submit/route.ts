import {NextRequest,NextResponse} from "next/server";import {createClient} from "@supabase/supabase-js";export async function POST(req:NextRequest){const body=await req.json();if(!body.title||!body.venue)return NextResponse.json({error:"Missing fields"},{status:400});if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.SUPABASE_SERVICE_ROLE_KEY)return NextResponse.json({ok:true,demo:true});const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);const {error}=await db.from("submissions").insert({title:body.title,venue:body.venue,category:body.category,source_url:body.source_url||null,status:"pending"});return NextResponse.json(error?{ok:false,error:error.message}:{ok:true});}