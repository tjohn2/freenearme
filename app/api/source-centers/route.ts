import {NextResponse} from "next/server";import {createClient} from "@supabase/supabase-js";
export async function GET(){const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL||"https://pixlqytdhgxcfgagwijy.supabase.co",process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||"sb_publishable_twXglFUicR3kgQ3fTO3pJA_Ji3qwIwm");const{data}=await db.from("source_registry").select("id,latitude,longitude,coverage_miles").eq("active",true);return NextResponse.json({sources:(data||[]).filter((x:any)=>Number.isFinite(Number(x.latitude))&&Number.isFinite(Number(x.longitude))).map((x:any)=>({id:x.id,lat:Number(x.latitude),lng:Number(x.longitude),radius:Math.min(25,Math.max(5,Number(x.coverage_miles||25))) }))},{headers:{"Cache-Control":"public, max-age=300"}})}