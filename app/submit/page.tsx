"use client";import {useState} from "react";export default function Submit(){const[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");async function go(e:any){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);const body:any=Object.fromEntries(f);try{if(navigator.geolocation){await new Promise<void>(resolve=>navigator.geolocation.getCurrentPosition(p=>{body.latitude=p.coords.latitude;body.longitude=p.coords.longitude;resolve()},()=>resolve(),{timeout:5000}))}const r=await fetch("/api/submit",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const j=await r.json();if(!r.ok||!j.ok)throw new Error(j.error||"Could not submit");setSent(true)}catch(err:any){setError(err.message)}finally{setBusy(false)}}return <main><header><button className="brand" onClick={()=>location.href="/"}>FREE<span>NEARME</span></button></header><section className="hero"><p className="eyebrow">HELP BUILD THE MAP OF FREE</p><h1>Found something<br/><em>free?</em></h1><p className="sub">Send the source. We verify it before anyone sees it.</p>{sent?<div className="card formcard"><h3>Hell yes. Submitted.</h3><p>It's in the verification queue.</p><a href="/">← Back to free stuff</a></div>:<form className="card formcard" onSubmit={go}><label>What's free?</label><input name="title" required maxLength={120} placeholder="Free coffee"/><label>Where?</label><input name="venue" required maxLength={120} placeholder="Business or venue"/><label>Address or area</label><input name="address" maxLength={200} placeholder="123 Main St or Downtown Gainesville"/><label>Source link</label><input name="source_url" type="url" required placeholder="https://…"/><label>Category</label><select name="category"><option>Food</option><option>Events</option><option>Kids</option><option>Activities</option><option>Admission</option><option>Stuff</option></select><label>When does it start?</label><input name="starts_at" type="datetime-local"/><label>Requirements</label><input name="requirements" maxLength={250} placeholder="First 100 people, app signup, etc."/><label>Anything else?</label><textarea name="description" maxLength={600} rows={4} placeholder="Helpful details…"/>{error&&<p className="formerror">{error}</p>}<button disabled={busy} className="submit">{busy?"Submitting…":"Submit for verification"}</button></form>}</section><style jsx>{`.formcard{display:grid;gap:10px;padding:24px}.formcard label{font-weight:900;margin-top:8px}.formcard input,.formcard select,.formcard textarea{font:inherit;padding:14px;border:1px solid #ccc;border-radius:10px;background:#fafafa}.formcard textarea{resize:vertical}.formerror{font-weight:800;color:#a31b1b}`}</style></main>}